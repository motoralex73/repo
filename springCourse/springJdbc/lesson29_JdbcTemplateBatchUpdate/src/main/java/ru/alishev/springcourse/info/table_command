create table Person (
    id int GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name varchar NOT NULL,
    age int check ( age > 0 ),
    email varchar UNIQUE
);
insert into person(name, age, email) values ('Bob',21,'bob@mail.ru');
drop table person;
select * from person;